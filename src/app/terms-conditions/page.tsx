import styles from "./page.module.scss";

export default function TermsConditions() {
  return (
    <section className={`${styles["terms-conditions"]}`}>
      <h2 className={`${styles["terms-conditions__title"]} heading-secundary`}>
        MAALUM POLICIES
      </h2>
      <article></article>
    </section>
  );
}
